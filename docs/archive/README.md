# Archive

Historical notes kept for reference. **These do not describe the current app.**

- `junie-notes/` — session summaries, plans and roadmaps written by the Junie
  coding agent during the CLI → Streamlit era (SMTP/Outlook COM, `email_from_list.py`).
  The app is now FastAPI + React with Microsoft Graph; see the root `README.md`.
- `user-requests.txt` — original wish list (OS / Material / Hardware). Still a
  useful source for the roadmap in the root `README.md`.
