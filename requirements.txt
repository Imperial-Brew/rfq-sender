# Core dependencies
Jinja2>=3.1.2
pyyaml>=6.0
pytest>=7.3.1
python-dotenv>=1.0.0  # For loading environment variables
bcrypt==4.0.1  # For password hashing (specific version to avoid compatibility issues)
streamlit
certifi

# Email handling
exchangelib==4.6.2  # For Exchange integration (specific version to avoid compatibility issues)
# pywin32>=305  # For Outlook integration (Windows only, no longer used)

# Optional but recommended
black>=23.3.0  # Code formatting
flake8>=6.0.0  # Linting
isort>=5.12.0  # Import sorting
pre-commit>=3.3.2  # Pre-commit hooks
mypy>=1.3.0  # Type checking

# CLI tools
questionary>=1.10.0  # Interactive command-line interface
rich>=12.0.0  # Terminal formatting and display