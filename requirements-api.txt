# Additional dependencies for the FastAPI layer.
# Your existing requirements.txt stays as-is for the Streamlit app.
# Install both: pip install -r requirements.txt -r requirements-api.txt

fastapi>=0.111.0
uvicorn[standard]>=0.30.1      # the server that runs FastAPI
python-jose[cryptography]>=3.3.0  # JWT encode/decode
